# LittleRip

Browser workspace for using connected cloud models.

## Web app

- `/` — landing page
- `/chat` — chat UI
- `/api/models` — lists configured models
- `/api/chat` — sends chat requests

Configure model/provider credentials in the site environment.
